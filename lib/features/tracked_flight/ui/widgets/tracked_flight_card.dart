import 'package:flutter/material.dart';

class TrackedFlightCard extends StatelessWidget {
  const TrackedFlightCard({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
